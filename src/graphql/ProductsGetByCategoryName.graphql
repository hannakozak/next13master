query ProductsGetByCategoryName(
	$name: String!
	$productsPerPage: Int!
	$offset: Int!
) {
	categories(where: { name: $name }) {
		products(first: $productsPerPage, skip: $offset) {
			...ProductListItem
		}
	}
}
